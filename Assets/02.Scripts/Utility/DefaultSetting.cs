using System;
using UnityEngine;

public class DefaultSetting : MonoBehaviour
{
    private void Awake()
    {
        // Cursor.visible = false;
        // Cursor.lockState = CursorLockMode.Locked;
    }
}
